# 自动节拍二次识别进度（2026-08-17）

## 本次完成内容

- 建立原始页面、HOMR 输入裁剪和版面侧车数据之间的坐标映射；自动修复只在可确认的同一谱表系统内工作。
- 自动候选依次尝试标准、对比度和上下文三个确定性图像版本。候选只有在节拍边界、音符头视觉证据和谱号、调号、拍号结构均通过验证时才可采用。
- 取消根据连线猜测系统左侧小节线的行为。左边界没有直接识别证据时，自动修复会保守地停止，不会修改 MusicXML。
- 将候选替换改为带提交日志的原子事务：先保存准备好的 XML、原始和结果摘要，再替换正式文件；服务重启后会仅在摘要匹配时完成或恢复该提交，绝不覆盖未知版本。
- 上传区域二次识别和人工候选选择共享提交日志及事务校验。上传仅在全部自动候选没有安全结果后才显示。
- 服务在“自动上传识别”阶段重启时，若 XML 已成功写入而批次状态尚未保存，会恢复为“等待审核”、刷新报告并将批次标为已采用；未形成可恢复提交的上传任务仍会明确标记为失败，不会假装继续执行。

## 验证记录

- `.venv\Scripts\python.exe -m unittest discover -s tests -v`：151 通过，2 跳过（需要外部 PDF/工具的集成测试）。
- `vendor\homr` 内运行 `..\..\.venv\Scripts\python.exe -m unittest tests.test_layout tests.test_staff_parsing -v`：15 通过。
- `node --test tests/review-state.test.cjs tests/job-session.test.cjs tests/auto-resolution-state.test.cjs`：20 通过。
- `node --check sheet2music/web/static/app.js`：通过。
- `git diff --check`：无差异格式错误；工作树使用 CRLF 时会输出换行符提示。

## Nautilus 留存任务的结论

- 留存任务：`8f7ec48483fd`。
- 自动二次识别共形成 18 个尚未解决的批次、54 次自动尝试；没有候选被自动采用，补图仍是最终兜底而非默认流程。
- `p2-s0-m14` 的裁剪实际包含同一行的两个完整小节，因此这一个失败不能归因于“下一行图像缺失”。
- 该曲在第一页有一个一拍弱起。页面 2 可见的印刷小节号 12 对应当前合并 MusicXML 的内部序号 13；从此处起，内部序号与印刷编号相差 1。
- 不得通过整体平移替换目标来修正这个差异，否则会替换到错误的小节。

## 后续计划

详细任务已追加到 `docs/superpowers/plans/2026-08-14-automatic-timing-reidentification.md` 的 Task 11：

1. 在布局、疑点、批次和审核数据中分离 `measure_ordinal`（内部 MusicXML 序号）与 `display_measure_number`（印刷编号）。
2. 保守检测弱起或隐式开头；无法确认时界面明确显示为内部序号。
3. 生成自动裁剪前验证目标内部序号确实处在该裁剪中，并在界面同时展示可确认的印刷编号。
4. 整行系统识别失败后，在同一行的已检测小节边界内做第二轮单小节或目标范围裁剪；尽量保留同一行锚点，绝不跨系统扩展上下文。
5. 为弱起后的第 12、13、14 个印刷小节和跨行边界添加回归测试，再重跑 18 个未解决批次，比较新的局部自动识别能消除多少上传需求。
